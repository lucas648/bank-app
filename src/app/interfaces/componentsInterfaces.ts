export interface ButtonItem {
  title?: string;
  innerText?: string;
  source?: string;
}

export interface goToItem {
  title?: string;
  text?: string;
  route?: string;
}

export interface login {
  login: string;
  password: string;
}